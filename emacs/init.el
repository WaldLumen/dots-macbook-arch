(require 'package)(setq package-archives '(("melpa" . "https://melpa.org/packages/")                         ("gnu" . "https://elpa.gnu.org/packages/")))(package-initialize)(unless (package-installed-p 'use-package)  (package-install 'use-package))(require 'use-package);; Установить прозрачность фрейма(set-frame-parameter (selected-frame) 'alpha-background 80) ;; Прозрачность фона(add-to-list 'default-frame-alist '(alpha-background . 80));; Установка и настройка doom-modeline(use-package doom-modeline  :ensure t  :init  (doom-modeline-mode 1)  :config  (setq doom-modeline-height 100)  (setq doom-modeline-bar-width 3)  (setq doom-modeline-minor-modes nil)  ; Скрыть минорные режимы (minions займётся этим)  (setq doom-modeline-icon t)  (setq doom-modeline-major-mode-icon t));; Установка шрифта(set-face-attribute 'default nil :font "JetBrains Mono Nerd Font" :height 130);; Общие улучшения(scroll-bar-mode -1)        ; Отключить скроллбар(tool-bar-mode -1)          ; Отключить тулбар(tooltip-mode -1)           ; Отключить всплывающие подсказки(menu-bar-mode -1)          ; Отключить меню(set-fringe-mode 10)        ; Отступы(custom-set-variables ;; custom-set-variables was added by Custom. ;; If you edit it by hand, you could mess it up, so be careful. ;; Your init file should contain only one such instance. ;; If there is more than one, they won't work right. '(package-selected-packages '(enlight minions doom-modeline moody)))(custom-set-faces ;; custom-set-faces was added by Custom. ;; If you edit it by hand, you could mess it up, so be careful. ;; Your init file should contain only one such instance. ;; If there is more than one, they won't work right. )(use-package doom-themes  :ensure t  :config  ;; Global settings (defaults)  (setq doom-themes-enable-bold t    ; if nil, bold is universally disabled        doom-themes-enable-italic t) ; if nil, italics is universally disabled  (load-theme 'doom-nord-light t)  ;; Enable flashing mode-line on errors  (doom-themes-visual-bell-config)  ;; Enable custom neotree theme (all-the-icons must be installed!)  (doom-themes-neotree-config)  ;; or for treemacs users  (setq doom-themes-treemacs-theme "doom-nord-light") ; use "doom-colors" for less minimal icon theme  (doom-themes-treemacs-config)  ;; Corrects (and improves) org-mode's native fontification.  (doom-themes-org-config));; Startpage(setopt initial-buffer-choice #'enlight)(require 'grid)(use-package enlight  :custom  (enlight-content   (grid-get-row    `(,(enlight-menu        '(("Org Mode"           ("Org-Agenda (current day)" (org-agenda nil "a") "a"))          ("Downloads"           ("Transmission" transmission "t")           ("Downloads folder" (dired "~/Downloads") "a"))          ("Other"           ("Projects" project-switch-project "p"))))      "   "      ,(enlight-menu        `(("Enlight Settings"           ("Jump to the config"            (progn              (find-file ,user-init-file)              (goto-char (point-min))              (search-forward "use-package enlight"))            "j")           ("Update"            (let ((beg (point)))              (with-current-buffer                  (find-file-noselect ,user-init-file)                (goto-char (point-min))                (search-forward "use-package enlight")                (eval-defun nil)                (enlight))              (goto-char beg))            "g"))          ("Important notes"           ("Month goals"  (find-file "~/documents/notes/self/mind/month_goals.org") "1")           ("Year goals"   (find-file "~/documents/notes/self/mind/year_goals.org") "2")           ("Global goals" (find-file "~/documents/notes/self/mind/global_goals.org") "3"))))))))